authorizations = {
  'Bearer': {
    'name': 'Authorization',
    'in': 'header',
    'type': 'apiKey',
    'description': 'Insira o seguinte conteúdo `Bearer JWT`, onde `JWT` é o token de autenticação',
  }
}
